algoritmo "Soma Pares"
var
  soma, par : real
inicio
soma:=0
par:=50
       enquanto par <= 100 faca
                soma := soma+par;
                par := par+2;
       fimenquanto;
       escreva(soma);
fimalgoritmo